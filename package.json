{
  "name": "weini-tutor-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Tutor SaaS platform with a React web dashboard and WeChat Mini Program client.",
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "preview": "npm run preview -w apps/web",
    "lint": "npm run lint -w apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
